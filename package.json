{
  "name": "chat-screen-app",
  "version": "1.0.0",
  "description": "a chat-screen application that sorts names and stuff",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "cd application && npm start",
    "db": "json-server --watch db/MOCK_DATA.json --port 8000"
  },
  "author": "Jared Jacobson",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.16.2"
  }
}
